// Local-dev placeholder — in production this file is regenerated at
// container start from real env vars (see docker-entrypoint.d/). Left
// empty here so env.ts falls back to Vite's build-time import.meta.env.
window.__ENV__ = window.__ENV__ || {};
